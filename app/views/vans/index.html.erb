<head>
  <%= stylesheet_link_tag 'van/index' %>
</head>
<%= render partial: 'layouts/banner'%>

<div class="container d-flex justify-content-center">
  <div class="logo-card">
    <%= image_tag("flexible.png", alt: "Logo 1", class: "logo-image") %>
    <div class="logo-text">
      <p>Flexible</p>
    </div>
  </div>
  <div class="logo-card">
    <%= image_tag("flexible.png", alt: "Logo 2", class: "logo-image") %>
    <div class="logo-text">
      <p>Fiable</p>
    </div>
  </div>
  <div class="logo-card">
    <%= image_tag("flexible.png", alt: "Logo 3", class: "logo-image") %>
    <div class="logo-text">
      <p>Fun</p>
    </div>
  </div>
    <div class="logo-card">
    <%= image_tag("flexible.png", alt: "Logo 4", class: "logo-image") %>
    <div class="logo-text">
      <p>Fonctionnel</p>
    </div>
  </div>
  <div class="logo-card">
    <%= image_tag("flexible.png", alt: "Logo 5", class: "logo-image") %>
    <div class="logo-text">
      <p>Français</p>
    </div>
  </div>
</div>

<div class="container">
  <div class="row p-5 m-5">
    <% if @vans.count <= 6%>
      <% @vans.sort_by(&:updated_at).reverse.each do |van| %>
        <div class="col-md-4 mb-4">
        <%= render partial: 'vans/van_card', locals: { van: van}  %>
        </div>
      <%end%>
    <% else %>
      <% @visible_vans[0..5].sort_by(&:updated_at).reverse.each do |van| %>
        <div class="col-md-4 mb-4">
          <%= render partial: 'vans/van_card', locals: { van: van}  %>
        </div>
      <% end %>
        <div style="display: block; margin: 0 auto;">
          <%= link_to "Voir plus de vans", full_index_vans_path, class:"btn btn-success btn-lg" %>
        </div>
    <%end%>
    <%= javascript_include_tag 'index' %>
  </div>
</div>
